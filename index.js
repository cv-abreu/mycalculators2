   

/*var num1 = Number(window.prompt("Escolha um número",""));

var num2 = Number(window.prompt("escolha outro número",""));

var operacao = window.prompt("qual operador: +  -  *  /","");


if (operacao==="+"){

    var resultado = num1+num2
}

if (operacao==="-"){

    var resultado = num1-num2
}

if (operacao==="*"){

    var resultado = num1*num2
}

if (operacao==="/"){

    var resultado = num1/num2
}

alert(resultado);